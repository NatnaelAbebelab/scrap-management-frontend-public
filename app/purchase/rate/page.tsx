import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Rate Management",
  description: "Manage purchase rates for different material types",
}

export { default } from './ratePage'
